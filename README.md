# SATCAT PARSER

A serverless function to run on **AWS Lambda** or as **Google Function** to fetch data from celestrak.com and build a JSON on firebase.

run localy with:
`serverless invoke local --function getsatcatboxscore`
